[project]
name = "citm-supervisor-webui"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=25.1.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
