[project]
name = "citm-mitmproxy-scripts"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "mako>=1.3.10",
    "mitmproxy>=11.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]

[tool.pytest.ini_options]
testpaths = ["rewrite_host", "mock_responder"]
